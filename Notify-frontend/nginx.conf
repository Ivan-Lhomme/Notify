server {
    listen 8000;
    root /usr/share/nginx/html;
    client_max_body_size 256M;

    location /api/ {
        proxy_pass http://notify-api:8080/;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}